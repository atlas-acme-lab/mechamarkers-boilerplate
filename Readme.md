# Mechamarkers-Boilerplate JS

## Setup
- `npm install`

## Start
- `npm run build`
- `npm start`
- Go to `localhost:8080/index.html` in a browser

## Using the Library
- Getting a marker: `Mechamarkers.getMarker(markerID)`
